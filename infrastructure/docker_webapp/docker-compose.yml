version: '3.8'

services:

  streamlit:
    image: streamlit/sl:1
    build:
      context: .
    ports:
      - "8080:8501"
    security_opt:
      - no-new-privileges:true
    read_only: true
    entrypoint: streamlit run app.py
